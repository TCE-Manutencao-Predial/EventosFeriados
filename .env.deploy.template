# =============================================================================
# CONFIGURAÇÃO DO EVENTOS E FERIADOS - TEMPLATE
# =============================================================================
# Este arquivo é o template para configuração do sistema.
# Para usar:
# 1. Copie este arquivo para .env.deploy
# 2. Preencha os valores das variáveis
# 3. O arquivo .env.deploy NÃO deve ser versionado (está no .gitignore)
# =============================================================================

# =============================================================================
# CONFIGURAÇÕES DO PROJETO
# =============================================================================

# Nome do projeto (usado em paths, logs, identificação)
PROJECT_NAME=eventos_feriados

# Nome de exibição do projeto (usado em interfaces)
PROJECT_NAME_DISPLAY="Sistema de Eventos e Feriados"

# Nome do serviço systemd
SERVICE_NAME=eventos_feriados.service

# Nome do módulo Python (para waitress-serve)
APP_NAME=eventos_feriados

# Porta do servidor WSGI
PORT=5045

# =============================================================================
# CAMINHOS DO SISTEMA
# =============================================================================

# Diretório raiz dos softwares TCE
ROOT_SOFTWARES=/var/softwaresTCE

# Diretório do backend (código da aplicação)
ROOT_BACKEND=/var/softwaresTCE/eventos_feriados

# Diretório de dados persistentes (JSONs, cache, status)
ROOT_DATA=/var/softwaresTCE/dados/eventos_feriados

# Diretório de logs
ROOT_LOGS=/var/softwaresTCE/logs/eventos_feriados

# Diretório de backups
BACKUP_DIR=/var/softwaresTCE/dados/eventos_feriados/backups

# =============================================================================
# CONFIGURAÇÕES FLASK
# =============================================================================

# Chave secreta do Flask (ALTERE em produção!)
SECRET_KEY=eventos_feriados_secret_key_CHANGE_THIS_IN_PRODUCTION

# Prefixo das rotas (ex: /EventosFeriados)
ROUTES_PREFIX=/EventosFeriados

# Modo debug (true/false) - SEMPRE false em produção!
DEBUG=false

# =============================================================================
# CONFIGURAÇÕES CLP PLENÁRIO (Feriados e Eventos)
# =============================================================================

# URL base da API do CLP
CLP_API_URL=https://automacao.tce.go.gov.br/scadaweb/api

# IP do CLP Térreo B1 (Plenário)
CLP_IP=172.17.85.104

# Credenciais de autenticação do CLP
CLP_AUTH_USER=eventosferiados
CLP_AUTH_PASS=WzPcMMFU

# Timeout para requisições HTTP ao CLP (segundos)
CLP_TIMEOUT=30

# Número de tentativas em caso de falha
CLP_RETRY_COUNT=3

# Horários de sincronização automática (separados por vírgula, formato HH:MM)
CLP_SYNC_TIMES=07:00,20:00

# Número máximo de feriados suportados pelo CLP
CLP_MAX_FERIADOS=20

# Número máximo de eventos do Plenário
CLP_MAX_EVENTOS_PLENARIO=10

# Habilitar sincronização automática (true/false)
CLP_SYNC_ENABLED=true

# =============================================================================
# CONFIGURAÇÕES CLP AUDITÓRIO
# =============================================================================

# IP do CLP Auditório AR
CLP_AUDITORIO_IP=172.17.85.123

# Número máximo de eventos do Auditório
CLP_MAX_EVENTOS_AUDITORIO=10

# Hora mínima para início (com ajuste automático de 1h antes)
# Formato HH:MM - eventos não podem começar antes deste horário
CLP_AUD_MIN_HORA=05:30

# Locais gerenciados pelo CLP Auditório (separados por vírgula)
CLP_AUD_LOCAIS="Auditório Nobre,Foyer do Auditório"

# =============================================================================
# CONFIGURAÇÕES API WHATSAPP (HelpDeskMonitor)
# =============================================================================

# Host da API de WhatsApp
WHATSAPP_API_HOST=https://automacao.tce.go.gov.br

# Token de autenticação
WHATSAPP_API_TOKEN=whatsapp_api_token_2025_helpdeskmonitor_tce

# Origem das mensagens (identificação)
WHATSAPP_API_ORIGEM=EVENTOS_FERIADOS

# Enviar apenas para usuários disponíveis (true/false)
WHATSAPP_APENAS_DISPONIVEIS=true

# Processamento assíncrono (true/false)
WHATSAPP_API_ASYNC=true

# Timeout para requisições (segundos)
WHATSAPP_API_TIMEOUT=60

# =============================================================================
# CONFIGURAÇÕES DE PAGINAÇÃO E CACHE
# =============================================================================

# Itens por página
ITEMS_PER_PAGE=20

# Tipo de cache (simple, redis, memcached)
CACHE_TYPE=simple

# Timeout padrão do cache (segundos)
CACHE_DEFAULT_TIMEOUT=300

# =============================================================================
# CONFIGURAÇÕES DE FORMATO
# =============================================================================

# Formato de data
DATE_FORMAT="%d/%m/%Y"

# Formato de hora
TIME_FORMAT="%H:%M"

# Formato de data e hora
DATETIME_FORMAT="%d/%m/%Y %H:%M"

# =============================================================================
# REPOSITÓRIO GIT
# =============================================================================

# Nome do repositório
GIT_REPO_NAME=eventos-feriados

# Proprietário do repositório
GIT_REPO_OWNER=TCE-Manutencao-Predial

# URL do repositório
GIT_REPO_URL=https://github.com/TCE-Manutencao-Predial/eventos-feriados.git

# =============================================================================
# CONFIGURAÇÕES DE DEPLOY
# =============================================================================

# Tempo de espera antes de reiniciar o serviço (segundos)
RESTART_SEC=10

# Habilitar serviço automaticamente no boot (true/false)
AUTO_HABILITAR_SERVICO=true

# Ambiente de execução (development, production, testing)
FLASK_ENV=production
